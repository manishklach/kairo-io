From 3333333333330000000000000000000000000000 Mon Sep 17 00:00:00 2001
From: Kairo POC <kairo-poc@local>
Date: Sun, 21 Jun 2026 14:10:00 -0700
Subject: [RFC/POC PATCH 3/9] io_uring: sketch Kairo hint plumbing

Subsystem touched:
- `include/uapi/linux/fs.h`
- `include/linux/fs.h`
- `io_uring/rw.c`
- `fs/read_write.c`

New Kairo concept:
- experimental userspace intent bits carried from `io_uring` or `preadv2` into
  `kiocb` and eventually request metadata

Relation to AI KV-cache traffic:
- decode, prefetch, prefill, and recomputable KV-cache paths need something more
  direct than temporary `ioprio` conventions

Temporary implementation:
- introduce RFC-only `RWF_KAIRO_*` flags and `IOCB_KAIRO_*` mirrors

Still to validate:
- whether the local POC should keep this as comments-only plumbing or turn it
  into a working out-of-tree hint path

diff --git a/include/uapi/linux/fs.h b/include/uapi/linux/fs.h
index 3417ea1..1ca20fa 100644
--- a/include/uapi/linux/fs.h
+++ b/include/uapi/linux/fs.h
@@
+#define RWF_KAIRO_DECODE	((__kernel_rwf_t)0x10000000)
+#define RWF_KAIRO_PREFETCH	((__kernel_rwf_t)0x20000000)
+#define RWF_KAIRO_PREFILL	((__kernel_rwf_t)0x40000000)
+#define RWF_KAIRO_RECOMPUTE	((__kernel_rwf_t)0x80000000)
+
 diff --git a/include/linux/fs.h b/include/linux/fs.h
 index 74a4c11..7d11b02 100644
 --- a/include/linux/fs.h
 +++ b/include/linux/fs.h
 @@
+#define IOCB_KAIRO_DECODE	BIT_ULL(33)
+#define IOCB_KAIRO_PREFETCH	BIT_ULL(34)
+#define IOCB_KAIRO_PREFILL	BIT_ULL(35)
+#define IOCB_KAIRO_RECOMPUTE	BIT_ULL(36)
@@
+static inline void kiocb_set_kairo_flags(struct kiocb *iocb, rwf_t rwf)
+{
+	if (rwf & RWF_KAIRO_DECODE)
+		iocb->ki_flags |= IOCB_KAIRO_DECODE;
+	if (rwf & RWF_KAIRO_PREFETCH)
+		iocb->ki_flags |= IOCB_KAIRO_PREFETCH;
+	if (rwf & RWF_KAIRO_PREFILL)
+		iocb->ki_flags |= IOCB_KAIRO_PREFILL;
+	if (rwf & RWF_KAIRO_RECOMPUTE)
+		iocb->ki_flags |= IOCB_KAIRO_RECOMPUTE;
+}
+
 diff --git a/fs/read_write.c b/fs/read_write.c
 index c1a77a0..42af1cc 100644
 --- a/fs/read_write.c
 +++ b/fs/read_write.c
 @@
  static ssize_t kiocb_set_rw_flags(struct kiocb *kiocb, rwf_t flags)
  {
+	/*
+	 * Local Kairo RFC/POC:
+	 * userspace intent could flow from preadv2/pwritev2 style flags into
+	 * kiocb flags, then down to bio/request metadata.
+	 */
+	kiocb_set_kairo_flags(kiocb, flags);
  }

 diff --git a/io_uring/rw.c b/io_uring/rw.c
 index 9ad0f9b..c8719a0 100644
 --- a/io_uring/rw.c
 +++ b/io_uring/rw.c
 @@
+static inline void io_kairo_sqe_to_kiocb(struct io_kiocb *req, struct kiocb *kiocb)
+{
+	/*
+	 * io_uring SQE -> kiocb flags -> bio/request metadata -> block scheduler
+	 * classification. Current benchmark still uses ioprio as the temporary
+	 * local signaling path until this hint plumbing is validated.
+	 */
+	kiocb_set_kairo_flags(kiocb, READ_ONCE(req->rw.flags));
+}
@@
  io_prep_rw(struct io_kiocb *req, const struct io_uring_sqe *sqe)
  {
+	io_kairo_sqe_to_kiocb(req, &req->rw.kiocb);
  }
